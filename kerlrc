KERL_CONFIGURE_OPTIONS="--with-dynamic-trace=dtrace --with-ssl=/usr/local --with-javac --disable-hipe --enable-native-libs --enable-kernel-poll --with-wx-config=/usr/local/bin/wxgtk3u-3.1-config --enable-threads --enable-sctp --enable-smp-support --enable-dirty-schedulers --enable-vm-probes --without-odbc "
export CFLAGS="$CFLAGS -DMAP_NORESERVE=0  -fno-omit-frame-pointer" 
export CC=clang CXX=clang CFLAGS="-g -O3 -fstack-protector" LDFLAGS="-fstack-protector"
export MAKE=gmake

